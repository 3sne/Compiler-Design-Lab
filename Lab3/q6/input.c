#include <stdio.h>

int main() {
    int a = 5;
    if ( you == bad ) {
        me = good;
    } else {
        me = bad;
    }
    printf(" either way some one is not good :( why tho ");
    if ( 3 < 2 && a == b) {
        yo
    }
}